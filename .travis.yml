language : python
python   :
           - "2.7"
           - "pypy"
         # command to install dependencies
install  :
           - pip install .
           - pip install -r requirements.txt
script   : 
           - rapoapp/manage.py test rapocore
